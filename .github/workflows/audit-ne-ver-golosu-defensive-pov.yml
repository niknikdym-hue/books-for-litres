name: Audit Ne ver golosu defensive POV

on:
  push:
    branches: [main]
    paths:
      - 'tools/audit_ne_ver_golosu_defensive_pov.py'
      - '.github/workflows/audit-ne-ver-golosu-defensive-pov.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan manuscript for attacker-centric framing
        run: python tools/audit_ne_ver_golosu_defensive_pov.py
