name: Audiobook Studio Offline

on:
  pull_request:
    paths:
      - "audiobook-studio/**"
      - ".github/workflows/audiobook-studio-offline.yml"
  push:
    branches:
      - main
    paths:
      - "audiobook-studio/**"
      - ".github/workflows/audiobook-studio-offline.yml"

permissions:
  contents: read

concurrency:
  group: audiobook-studio-offline-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  offline-tests:
    name: Python offline safety suite
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: audiobook-studio
    env:
      OPENAI_API_KEY: ""
      YANDEX_API_KEY: ""
      YANDEX_CLOUD_API_KEY: ""
      PYTHONDONTWRITEBYTECODE: "1"

    steps:
      - name: Checkout exact revision
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install offline test dependency
        run: python -m pip install 'numpy>=2,<3'

      - name: Targeted Chapter Production tests
        run: |
          python -m unittest discover -s tests -p 'test_chapter_production.py' -v
          python -m unittest discover -s tests -p 'test_qwen_chapter_manifest.py' -v
          python -m unittest discover -s tests -p 'test_qwen_chapter_execution.py' -v
          python -m unittest discover -s tests -p 'test_qwen_chapter_runner.py' -v
          python -m unittest discover -s tests -p 'test_yandex_chapter_plan.py' -v
          python -m unittest discover -s tests -p 'test_yandex_chapter_runner.py' -v
          python -m unittest discover -s tests -p 'test_yandex_speechkit.py' -v

      - name: Full Audiobook Studio offline regression suite
        run: python -m unittest discover -s tests -p 'test_*.py' -v
